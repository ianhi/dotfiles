// ===================================================================
// Zellij Configuration
// Matches: kitty + neovim + Tokyo Night + vim keybindings
// ===================================================================

// ----- Theme -----
themes {
    tokyo-night {
        fg "#c0caf5"
        bg "#1a1b26"
        black "#15161e"
        red "#f7768e"
        green "#9ece6a"
        yellow "#e0af68"
        blue "#7aa2f7"
        magenta "#bb9af7"
        cyan "#7dcfff"
        white "#a9b1d6"
        orange "#ff9e64"
    }
}
theme "tokyo-night"

// ----- General -----
default_layout "clean"
pane_frames false
scroll_buffer_size 50000
copy_on_select true
copy_command "pbcopy"
copy_clipboard "system"
scrollback_editor "nvim"
on_force_close "detach"
session_serialization true
support_kitty_keyboard_protocol true
simplified_ui false
show_release_notes false

// ----- Keybindings -----
// Two ways to do things:
//   1. Tmux-style: Ctrl+A then key (quick one-shot actions)
//   2. Native modes: Ctrl+P/T/N/S for sustained pane/tab/resize/scroll
// Ctrl+hjkl navigates panes seamlessly (autolock passes them to nvim when focused).
keybinds clear-defaults=true {
    // --- Normal mode: re-trigger autolock after shell commands ---
    normal {
        bind "Enter" { WriteChars "\u{000D}"; MessagePlugin "autolock" {}; }
    }

    // --- Locked mode: everything passes through except the unlock key ---
    locked {
        bind "Ctrl g" { SwitchToMode "normal"; }
        bind "Alt z" {
            MessagePlugin "autolock" { payload "disable"; };
            SwitchToMode "normal";
        }
    }

    // --- Tmux mode (prefix: Ctrl+A) ---
    tmux {
        bind "Ctrl a" { Write 1; SwitchToMode "normal"; }
        // Splits
        bind "v" { NewPane "right"; SwitchToMode "normal"; }
        bind "s" { NewPane "down"; SwitchToMode "normal"; }
        bind "\"" { NewPane "down"; SwitchToMode "normal"; }
        bind "%" { NewPane "right"; SwitchToMode "normal"; }
        // Pane navigation
        bind "h" { MoveFocus "left"; SwitchToMode "normal"; }
        bind "j" { MoveFocus "down"; SwitchToMode "normal"; }
        bind "k" { MoveFocus "up"; SwitchToMode "normal"; }
        bind "l" { MoveFocus "right"; SwitchToMode "normal"; }
        bind "Left" { MoveFocus "left"; SwitchToMode "normal"; }
        bind "Down" { MoveFocus "down"; SwitchToMode "normal"; }
        bind "Up" { MoveFocus "up"; SwitchToMode "normal"; }
        bind "Right" { MoveFocus "right"; SwitchToMode "normal"; }
        // Tabs
        bind "c" { NewTab; SwitchToMode "normal"; }
        bind "n" { GoToNextTab; SwitchToMode "normal"; }
        bind "p" { GoToPreviousTab; SwitchToMode "normal"; }
        bind "1" { GoToTab 1; SwitchToMode "normal"; }
        bind "2" { GoToTab 2; SwitchToMode "normal"; }
        bind "3" { GoToTab 3; SwitchToMode "normal"; }
        bind "4" { GoToTab 4; SwitchToMode "normal"; }
        bind "5" { GoToTab 5; SwitchToMode "normal"; }
        bind "6" { GoToTab 6; SwitchToMode "normal"; }
        bind "7" { GoToTab 7; SwitchToMode "normal"; }
        bind "8" { GoToTab 8; SwitchToMode "normal"; }
        bind "9" { GoToTab 9; SwitchToMode "normal"; }
        bind "," { SwitchToMode "renametab"; TabNameInput 0; }
        // Pane management
        bind "x" { CloseFocus; SwitchToMode "normal"; }
        bind "z" { ToggleFocusFullscreen; SwitchToMode "normal"; }
        bind "f" { ToggleFloatingPanes; SwitchToMode "normal"; }
        bind "o" { FocusNextPane; }
        bind "Space" { NextSwapLayout; }
        // Scroll/copy mode
        bind "[" { SwitchToMode "scroll"; }
        bind "Enter" { SwitchToMode "scroll"; }
        // Session
        bind "d" { Detach; }
        bind "w" {
            LaunchOrFocusPlugin "session-manager" {
                floating true
                move_to_focused_tab true
            }
            SwitchToMode "normal"
        }
        // Quick resize
        bind "H" { Resize "Increase left"; }
        bind "J" { Resize "Increase down"; }
        bind "K" { Resize "Increase up"; }
        bind "L" { Resize "Increase right"; }
        // Enter native modes for sustained operations
        bind "P" { SwitchToMode "pane"; }
        bind "T" { SwitchToMode "tab"; }
        bind "N" { SwitchToMode "resize"; }
        bind "S" { SwitchToMode "scroll"; }
        bind "O" { SwitchToMode "session"; }
    }

    // --- Native pane mode (Ctrl+P): sustained pane operations ---
    pane {
        bind "Ctrl p" { SwitchToMode "normal"; }
        bind "h" "Left" { MoveFocus "left"; }
        bind "j" "Down" { MoveFocus "down"; }
        bind "k" "Up" { MoveFocus "up"; }
        bind "l" "Right" { MoveFocus "right"; }
        bind "n" { NewPane; SwitchToMode "normal"; }
        bind "d" { NewPane "down"; SwitchToMode "normal"; }
        bind "r" { NewPane "right"; SwitchToMode "normal"; }
        bind "x" { CloseFocus; SwitchToMode "normal"; }
        bind "f" { ToggleFocusFullscreen; SwitchToMode "normal"; }
        bind "w" { ToggleFloatingPanes; SwitchToMode "normal"; }
        bind "s" { NewPane "stacked"; SwitchToMode "normal"; }
        bind "e" { TogglePaneEmbedOrFloating; SwitchToMode "normal"; }
        bind "c" { SwitchToMode "renamepane"; PaneNameInput 0; }
        bind "Esc" "Enter" { SwitchToMode "normal"; }
    }

    // --- Native tab mode (Ctrl+T): sustained tab operations ---
    tab {
        bind "Ctrl t" { SwitchToMode "normal"; }
        bind "h" "Left" "k" "Up" { GoToPreviousTab; }
        bind "l" "Right" "j" "Down" { GoToNextTab; }
        bind "n" { NewTab; SwitchToMode "normal"; }
        bind "x" { CloseTab; SwitchToMode "normal"; }
        bind "r" { SwitchToMode "renametab"; TabNameInput 0; }
        bind "s" { ToggleActiveSyncTab; SwitchToMode "normal"; }
        bind "b" { BreakPane; SwitchToMode "normal"; }
        bind "]" { BreakPaneRight; SwitchToMode "normal"; }
        bind "[" { BreakPaneLeft; SwitchToMode "normal"; }
        bind "1" { GoToTab 1; SwitchToMode "normal"; }
        bind "2" { GoToTab 2; SwitchToMode "normal"; }
        bind "3" { GoToTab 3; SwitchToMode "normal"; }
        bind "4" { GoToTab 4; SwitchToMode "normal"; }
        bind "5" { GoToTab 5; SwitchToMode "normal"; }
        bind "6" { GoToTab 6; SwitchToMode "normal"; }
        bind "7" { GoToTab 7; SwitchToMode "normal"; }
        bind "8" { GoToTab 8; SwitchToMode "normal"; }
        bind "9" { GoToTab 9; SwitchToMode "normal"; }
        bind "Tab" { ToggleTab; }
        bind "Esc" "Enter" { SwitchToMode "normal"; }
    }

    // --- Native resize mode (Ctrl+N): repeatable resizing ---
    resize {
        bind "Ctrl n" { SwitchToMode "normal"; }
        bind "h" "Left" { Resize "Increase left"; }
        bind "j" "Down" { Resize "Increase down"; }
        bind "k" "Up" { Resize "Increase up"; }
        bind "l" "Right" { Resize "Increase right"; }
        bind "H" { Resize "Decrease left"; }
        bind "J" { Resize "Decrease down"; }
        bind "K" { Resize "Decrease up"; }
        bind "L" { Resize "Decrease right"; }
        bind "=" "+" { Resize "Increase"; }
        bind "-" { Resize "Decrease"; }
        bind "Esc" "Enter" { SwitchToMode "normal"; }
    }

    // --- Scroll mode (vim-style) ---
    scroll {
        bind "Ctrl s" { SwitchToMode "normal"; }
        bind "j" "Down" { ScrollDown; }
        bind "k" "Up" { ScrollUp; }
        bind "d" "Ctrl d" { HalfPageScrollDown; }
        bind "u" "Ctrl u" { HalfPageScrollUp; }
        bind "Ctrl f" "PageDown" { PageScrollDown; }
        bind "Ctrl b" "PageUp" { PageScrollUp; }
        bind "G" { ScrollToBottom; }
        bind "/" "s" { SwitchToMode "entersearch"; SearchInput 0; }
        bind "e" { EditScrollback; SwitchToMode "normal"; }
        bind "Esc" "q" { SwitchToMode "normal"; }
    }

    // --- Search mode ---
    search {
        bind "Ctrl s" { SwitchToMode "normal"; }
        bind "j" "Down" { ScrollDown; }
        bind "k" "Up" { ScrollUp; }
        bind "d" "Ctrl d" { HalfPageScrollDown; }
        bind "u" "Ctrl u" { HalfPageScrollUp; }
        bind "n" { Search "down"; }
        bind "N" "p" { Search "up"; }
        bind "c" { SearchToggleOption "CaseSensitivity"; }
        bind "w" { SearchToggleOption "Wrap"; }
        bind "o" { SearchToggleOption "WholeWord"; }
        bind "Esc" "q" { SwitchToMode "normal"; }
    }

    entersearch {
        bind "Ctrl c" "Esc" { SwitchToMode "scroll"; }
        bind "Enter" { SwitchToMode "search"; }
    }

    renametab {
        bind "Ctrl c" { SwitchToMode "normal"; }
        bind "Esc" { UndoRenameTab; SwitchToMode "normal"; }
    }

    renamepane {
        bind "Ctrl c" { SwitchToMode "normal"; }
        bind "Esc" { UndoRenamePane; SwitchToMode "normal"; }
    }

    // --- Session mode (Ctrl+O) ---
    session {
        bind "Ctrl o" { SwitchToMode "normal"; }
        bind "d" { Detach; }
        bind "w" {
            LaunchOrFocusPlugin "session-manager" {
                floating true
                move_to_focused_tab true
            }
            SwitchToMode "normal"
        }
        bind "Esc" "Enter" { SwitchToMode "normal"; }
    }

    // --- Shared bindings ---
    shared_except "locked" "tmux" {
        bind "Ctrl a" { SwitchToMode "tmux"; }
    }

    shared_except "locked" {
        bind "Ctrl g" { SwitchToMode "locked"; }
    }

    // Native modes accessible via Ctrl+A then mode key
    // (not bound globally - would conflict with apps like Claude Code)

    // Ctrl+hjkl for pane navigation (autolock passes these to nvim when focused)
    shared_except "locked" {
        bind "Ctrl h" { MoveFocusOrTab "left"; }
        bind "Ctrl j" { MoveFocus "down"; }
        bind "Ctrl k" { MoveFocus "up"; }
        bind "Ctrl l" { MoveFocusOrTab "right"; }
        bind "Alt n" { NewPane; }
        bind "Alt f" { ToggleFloatingPanes; }
        bind "Alt z" {
            MessagePlugin "autolock" { payload "disable"; };
            SwitchToMode "locked";
        }
    }

    shared {
        bind "Alt Shift z" {
            MessagePlugin "autolock" { payload "enable"; };
        }
    }
}

// ----- Plugins -----
plugins {
    tab-bar location="zellij:tab-bar"
    status-bar location="zellij:status-bar"
    compact-bar location="zellij:compact-bar"
    session-manager location="zellij:session-manager"
    welcome-screen location="zellij:session-manager" {
        welcome_screen true
    }
    configuration location="zellij:configuration"
    plugin-manager location="zellij:plugin-manager"
    autolock location="file:~/.config/zellij/plugins/zellij-autolock.wasm" {
        is_enabled true
        triggers "nvim|vim|git|fzf|zoxide|atuin"
        reaction_seconds "0.3"
        print_to_log false
    }
}

load_plugins {
    autolock
}

